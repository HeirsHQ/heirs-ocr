import { ChangelogContent } from "@/components/docs";
import { PageLayout } from "@/components/shared";

const Page = () => (
  <PageLayout title="Changelog" subtitle="What's new in the Heirs OCR service.">
    <ChangelogContent />
  </PageLayout>
);

export default Page;
